---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: multidimpodautoscalers.autoscaling.x-k8s.io
spec:
  group: autoscaling.x-k8s.io
  names:
    kind: MultidimPodAutoscaler
    listKind: MultidimPodAutoscalerList
    plural: multidimpodautoscalers
    shortNames:
    - mpa
    singular: multidimpodautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .status.activeAutoscaler.name
      name: Active Autoscaler
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MultidimPodAutoscaler coordinates all HPAs and VPAs that target the same
          workload. The user declares intent via spec.targetRef; the controller
          discovers the actual scalers and records them in status.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MultidimPodAutoscalerSpec defines the desired state of a
              MultidimPodAutoscaler.
            properties:
              stableDurationSeconds:
                description: |-
                  StableDurationSeconds is the minimum number of seconds all HPAs must
                  have been stable (currentReplicas == desiredReplicas) before the
                  controller allows a VPA to become active. This prevents VPA from
                  resuming evictions while the fleet is still settling after a scale event.
                  Defaults to 60.
                format: int32
                type: integer
              targetRef:
                description: |-
                  TargetRef points to the workload being scaled.
                  The controller uses this to discover all HPAs and VPAs in the same
                  namespace whose scaleTargetRef / targetRef matches this reference.
                properties:
                  apiVersion:
                    description: apiVersion is the API version of the referent
                    type: string
                  kind:
                    description: 'kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                    type: string
                  name:
                    description: 'name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              tokenHoldDurationSeconds:
                description: |-
                  TokenHoldDurationSeconds is the maximum number of seconds a single VPA
                  may remain the active autoscaler before the controller re-evaluates
                  which VPA should hold the token. This prevents a high-priority VPA from
                  monopolising actuation indefinitely when multiple VPAs target the same
                  workload. After the token expires the controller re-selects the
                  highest-priority VPA; if the winner is unchanged the token is simply
                  renewed. Defaults to 600.
                format: int32
                type: integer
            required:
            - targetRef
            type: object
          status:
            description: MultidimPodAutoscalerStatus describes the current state of
              a MultidimPodAutoscaler.
            properties:
              activeAutoscaler:
                description: |-
                  ActiveAutoscaler is the name and type of the scaler currently allowed
                  to act. Nil when no scaler is active (e.g., between an HPA stabilizing
                  and VPA resuming after stableDurationSeconds).
                properties:
                  inProgress:
                    description: |-
                      InProgress indicates whether this scaler is currently active.
                      For HPA: true when desiredReplicas != currentReplicas.
                      For VPA: true when a recommendation has been provided (RecommendationProvided condition).
                    type: boolean
                  name:
                    description: Name is the name of the HPA or VPA object.
                    type: string
                  scalerType:
                    description: ScalerType is the kind of the referenced scaler.
                    enum:
                    - HorizontalPodAutoscaler
                    - VerticalPodAutoscaler
                    type: string
                required:
                - inProgress
                - name
                - scalerType
                type: object
              autoscalers:
                description: |-
                  AutoScalers lists the HorizontalPodAutoscalers and VerticalPodAutoscalers
                  discovered by the controller as co-targeting the same workload as this
                  MPA. Updated on every reconcile.
                items:
                  description: AutoScalerRef identifies a single HPA or VPA object
                    tracked by the MPA.
                  properties:
                    inProgress:
                      description: |-
                        InProgress indicates whether this scaler is currently active.
                        For HPA: true when desiredReplicas != currentReplicas.
                        For VPA: true when a recommendation has been provided (RecommendationProvided condition).
                      type: boolean
                    name:
                      description: Name is the name of the HPA or VPA object.
                      type: string
                    scalerType:
                      description: ScalerType is the kind of the referenced scaler.
                      enum:
                      - HorizontalPodAutoscaler
                      - VerticalPodAutoscaler
                      type: string
                  required:
                  - inProgress
                  - name
                  - scalerType
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              conditions:
                description: Conditions describes the current state of the MultidimPodAutoscaler.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastTransitionTime:
                description: LastTransitionTime is the last time the active autoscaler
                  changed.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
