apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: hamster-vpa-high
  namespace: mpa-demo
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hamster
  # priority 0 (default) — highest priority; this VPA wins the MPA token.
  updatePolicy:
    # Recreate: the VPA updater evicts pods so new ones are admitted with the
    # recommended resource requests.  Visible effect: pods are restarted and
    # their requests change to match the recommendation.
    updateMode: Recreate
  resourcePolicy:
    containerPolicies:
    - containerName: hamster
      minAllowed:
        cpu: 50m
        memory: 64Mi
      maxAllowed:
        cpu: "1"
        memory: 512Mi
      # Control which resources the VPA manages.
      controlledResources: ["cpu", "memory"]
